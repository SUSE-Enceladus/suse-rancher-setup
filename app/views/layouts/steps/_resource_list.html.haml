%hr
- if defined?(title)
  %h3= title
- if defined?(caption)
  = markdown(caption)
- if resources.length > 0
  %ul
    - resources.each do |resource|
      %li
        = friendly_type(resource.type)
        = resource.id
- else
  = markdown(t('resources.none'))
