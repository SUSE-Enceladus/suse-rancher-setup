= page_header(t('engines.aws.steps.title'))
-# body
%h3= @step
%pre= @output
-# end
= render('layouts/navigation_buttons') do
  = link_to(t('actions.previous'), aws.new_step_path, class: 'btn btn-secondary')
  = next_step_button(aws.new_step_path)
