= page_header(t('engines.aws.credentials.title'))
-# body
%p
  Using AWS Acccess Key:
  %span.text-monospace= @credential.aws_access_key_id
-# end
= render('layouts/navigation_buttons') do
  = link_to(t('Back'), edit_credential_path(), class: 'btn btn-secondary')
  = next_step_button(edit_credential_path())
