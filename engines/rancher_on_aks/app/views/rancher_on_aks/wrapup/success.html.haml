= page_header(t('engines.rancher_on_aks.wrapup.title'))
-# body
= markdown(t('engines.rancher_on_aks.wrapup.caption'))
= markdown(t('engines.rancher_on_aks.wrapup.url', fqdn: @fqdn))
= markdown(t('engines.rancher_on_aks.wrapup.password', password: @password))
= markdown(t('engines.rancher_on_aks.wrapup.more_info', resource_group: @resource_group, cluster: @cluster))
= render('resource_cleanup', resources: @resources)
