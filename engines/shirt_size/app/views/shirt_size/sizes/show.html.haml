= page_header(t('engines.shirt_size.sizes.title'))
-# body
%p
  = t('engines.shirt_size.sizes.using', size: @size)
-# end
= render('layouts/navigation_buttons') do
  = link_to(t('actions.previous'), shirt_size.edit_size_path, class: 'btn btn-secondary')
  = next_step_button(shirt_size.edit_size_path)
